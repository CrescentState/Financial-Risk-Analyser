{
  "name": "financial-risk-analyser",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"python -m uvicorn main:app --port 8000 --reload\" \"cd frontend && npm run dev\"",
    "backend": "python -m uvicorn main:app --port 8000 --reload",
    "frontend": "cd frontend && npm run dev",
    "build:frontend": "cd frontend && npm install && npm run build",
    "install:frontend": "cd frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}